请帮我用 Go 和 Vue 实现一个完整的即时通讯系统，代码要可以直接运行。

后端要求：
- Go + Gin + WebSocket + SQLite，不需要 Redis（用 Go 本地缓存代替，比如一个简单的 sync.Map 或 go-cache 存在线状态和未读数）
- 实现 JWT 登录注册
- 单聊：文本消息、图片消息
- 群聊：创建群、拉人、踢人、发群消息
- 消息存 SQLite，在线状态和未读数用本地缓存
- 离线消息：上线后自动拉取
- 已读回执
- WebSocket 连接管理（Hub 模式），支持心跳检测
- 后端代码放到 server 目录

前端要求：
- Vue 3 + TypeScript + Vite + Pinia
- 页面：登录页、好友列表、聊天窗口
- WebSocket 封装：自动重连、心跳
- 聊天窗口显示消息气泡，支持发送文本和图片
- 有新消息时桌面通知
- 前端代码放到 web 目录

输出内容：
1. 完整的目录结构（server 和 web 分开）
2. 后端所有核心代码（server/main.go、handler、service、model、websocket、storage、cache）
3. 前端所有核心代码（web/src 下的 App、聊天组件、WebSocket composable、store）
4. 数据库建表 SQL（SQLite 语法：users、friends、groups、group_members、messages）
5. 启动说明（如何运行 server 和 web）
6. 接口都要跑下测试用例，前端也要跑下测试用例。

注意：不用 Redis，用 Go 本地缓存。不要用第三方 IM SDK，从零实现。代码要完整能跑起来。

请先熟悉我现有的 IM 聊天系统功能，在此基础上完成以下迭代需求：
1. 用户头像功能：
   - 注册时支持上传个人头像
   - 注册后可在个人资料编辑页面修改头像

2. 界面头像展示：
   - 右侧栏（好友列表/联系人列表）展示用户头像
   - 聊天窗口中将原有的用户名展示替换为头像展示

3. 消息功能增强：
   - 发送消息时支持插入 Emoji 表情
   - 发送消息时支持选择并发送图片

4. 后台管理功能：
   - 新增聊天数据列表页面
   - 展示所有用户的聊天记录
   - 支持分页，每页显示 20 条数据


请帮我基于现有的 Go + Vue 即时通讯系统，增加后台群主管理功能。

## 功能需求

### 1. 后台管理端（新增）
- 新增群主管理页面，支持创建群主账号
- 创建群主时需要填写：群主名称、群主简介、群主头像（可选）
- 创建成功后，该群主自动成为一个特殊群组的拥有者

### 2. 前台展示
- 普通用户登录后，在联系人列表或群组列表中可以看到后台创建的群主
- 群主以特殊标识显示（如带“官方”或“群主”角标）

### 3. 加入群主
- 用户可以直接加入群主（类似关注或加入群聊）
- 加入后，该群主会出现在用户的群聊列表中

### 4. 群主聊天
- 多个用户加入同一个群主后，可以在该群主下进行群聊
- 群主本身可以发言，所有加入的用户都能看到
- 实现群主与用户之间的群聊功能

### 5. 群主管理（后台增强）
- 查看某个群主下的所有成员列表
- 支持将某个成员踢出群主群聊
- 支持解散群主（删除群主及所有成员关系）

## 数据库改动

需要新增或修改以下表：

1. **owners 表**：存储群主信息
   - id、name、description、avatar、created_at

2. **owner_members 表**：存储用户与群主的加入关系
   - id、owner_id、user_id、joined_at

3. 或者复用现有的 groups 和 group_members 表，增加一个 is_official 字段标识群主

## 输出内容

1. 后端新增代码：
   - owner handler、service、model
   - 创建群主 API、加入群主 API、获取群主列表 API、踢出成员 API、解散群主 API
   - WebSocket 群主消息路由

2. 前端新增代码：
   - 后台管理页面（群主列表、创建群主、成员管理）
   - 前台群主列表组件、加入群主按钮、群主聊天窗口



